# ── Database
DATABASE_URL=postgresql://mangorack:CHANGE_ME_BEFORE_DEPLOYING@localhost:5432/mangorack
POSTGRES_PASSWORD=CHANGE_ME_BEFORE_DEPLOYING

# ── Redis
REDIS_PASSWORD=changeme_redis
REDIS_URL=redis://:changeme_redis@localhost:6379

# ── Auth (REQUIRED - generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-secret-here-min-32-chars
NEXTAUTH_URL=http://localhost:3000

# ── App
PORT=3000
NODE_ENV=production
